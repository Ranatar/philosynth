/**
 * Промптовые шаблоны Element Enrichment (беседа 5.3; 01-arch §4.9,
 * 03-spec §2.14 — канон ключей 2026-09-02). НОВЫЕ тексты: в исходнике
 * подсистемы нет, генератора extract-seed-data.mjs для них нет — файл
 * правится вручную, сеется scripts/seed-prompts.ts вместе с 0.3/1.2.
 *
 * Схема ключей: enrichment.{элемент}.{тип}; ключей ШЕСТЬ (03 §2.14 пишет
 * «пять ключей» и перечисляет шесть — счёт в доке ошибочен, состав верен):
 *   enrichment.category.description | .evolution | .justification
 *   enrichment.edge.justification | .counterarguments
 *   enrichment.characteristic_justification
 *
 * Системный промпт — buildSYS(p, { outputMode: "mode" }) (прецедент 4.1):
 * инструкция формата вывода «задана в задании ниже» — поэтому каждый
 * шаблон завершается блоком «ФОРМАТ ОТВЕТА». Язык вывода — lang синтеза
 * через system.lang_instruction (в самих шаблонах языка нет).
 *
 * Плейсхолдеры (все обязательны — renderTemplate strict):
 *   общие:      {{synthesis_context}}
 *   категория:  {{category_name}} {{category_type}} {{category_definition}}
 *               {{category_origin}} {{category_roles}} {{category_metrics}}
 *               {{related_edges}}
 *   связь:      {{source_name}} {{source_definition}} {{target_name}}
 *               {{target_definition}} {{edge_type}} {{edge_direction}}
 *               {{edge_description}} {{edge_metrics}}
 *   характеристика: {{element_kind}} {{element_label}} {{element_summary}}
 *               {{characteristic_label}} {{characteristic_key}} {{value}}
 *               {{range}} {{current_value}}
 *
 * Разбор ответа обоснования характеристики — по data-section
 * (element-enrichment.ts, JUSTIFICATION_SECTIONS): «Основания» →
 * justification, «Ограничения» → limitations, «Альтернативные подходы» →
 * alternative_approaches. Менять названия секций здесь без правки
 * констант сервиса нельзя.
 */
import type { SeedPromptTemplate } from "./prompt-templates.js";

/** Названия data-section ответа обоснования характеристики (02 §2.27). */
export const JUSTIFICATION_SECTIONS = {
  justification: "Основания",
  limitations: "Ограничения",
  alternatives: "Альтернативные подходы",
} as const;

const CONTEXT_BLOCK = `КОНТЕКСТ СИНТЕЗА:
"""
{{synthesis_context}}
"""`;

const FORMAT_FRAGMENT = `ФОРМАТ ОТВЕТА: ТОЛЬКО HTML-фрагмент вида <div class="enrichment-result">…</div>. Внутри допустимы <h5>, <p>, <ul>/<ol> с <li>, <strong>, <em>, <table class="doc-table">. Никакой обёртки <div class="doc-section">, никакого текста вне фрагмента. Ссылки на разделы документа — явные (§ и названия категорий).`;

export const SEED_ENRICHMENT_TEMPLATES: SeedPromptTemplate[] = [
  {
    key: "enrichment.category.description",
    body: `Задача: расширенное описание одной категории синтезированной философской концепции — без перегенерации разделов документа.

${CONTEXT_BLOCK}

КАТЕГОРИЯ: «{{category_name}}»
Тип: {{category_type}}
Определение (из графа): {{category_definition}}
Происхождение/генеалогия: {{category_origin}}
Роли в топологии: {{category_roles}}
Характеристики: {{category_metrics}}
Связи в графе:
{{related_edges}}

Требуется (каждый пункт — отдельный <h5> и содержательный текст не менее 120 слов):
1. Развёрнутое описание: что категория схватывает в данной концепции, как определение уточняется её связями и ролями.
2. Альтернативные трактовки: не менее трёх способов понять категорию иначе (внутри той же концепции или с позиций участников синтеза), с указанием, что каждая трактовка меняет в графе.
3. Исторические аналоги: ближайшие понятия в истории философии (не менее трёх), с точным указанием сходства и ключевого различия.
4. Связанные концепты: понятия, которые категория предполагает, порождает или вытесняет, — с опорой на связи из графа.

${FORMAT_FRAGMENT}`,
    description:
      "Enrichment (5.3): расширенное описание категории — трактовки, исторические аналоги, связанные концепты (01-arch §4.9, тип description)",
  },
  {
    key: "enrichment.category.evolution",
    body: `Задача: анализ эволюции одной категории синтезированной философской концепции — как она может измениться в свете современных тенденций.

${CONTEXT_BLOCK}

КАТЕГОРИЯ: «{{category_name}}»
Тип: {{category_type}}
Определение (из графа): {{category_definition}}
Происхождение/генеалогия: {{category_origin}}
Роли в топологии: {{category_roles}}
Характеристики: {{category_metrics}}
Связи в графе:
{{related_edges}}

Требуется (каждый пункт — отдельный <h5>, не менее 120 слов):
1. Векторы давления: какие современные философские, научные и культурные тенденции (не менее трёх, названные конкретно) ставят категорию под вопрос или требуют её переопределения.
2. Сценарии эволюции: не менее трёх сценариев изменения категории (уточнение, расщепление, поглощение соседней категорией, утрата центральности и т.п.) — для каждого: триггер, новое определение, следствия для связей графа.
3. Инварианты: что в категории должно сохраниться при любом сценарии, чтобы концепция осталась собой.
4. Оценка: какой сценарий наиболее вероятен и почему; какая характеристика (центральность, определённость, инновационность) изменится первой.

${FORMAT_FRAGMENT}`,
    description:
      "Enrichment (5.3): эволюция категории — векторы давления, сценарии изменения, инварианты (тип evolution)",
  },
  {
    key: "enrichment.category.justification",
    body: `Задача: философское обоснование одной категории синтезированной концепции — почему она необходима именно в этом виде.

${CONTEXT_BLOCK}

КАТЕГОРИЯ: «{{category_name}}»
Тип: {{category_type}}
Определение (из графа): {{category_definition}}
Происхождение/генеалогия: {{category_origin}}
Роли в топологии: {{category_roles}}
Характеристики: {{category_metrics}}
Связи в графе:
{{related_edges}}

Требуется (каждый пункт — отдельный <h5>, не менее 120 слов):
1. Необходимость: какие проблемы или напряжения концепции не решаются без этой категории; что рухнет в графе при её удалении.
2. Обоснование определения: почему определение сформулировано так, а не иначе — какие альтернативные формулировки отвергнуты и по какой причине.
3. Обоснование типа и ролей: почему категория отнесена к типу «{{category_type}}» и несёт указанные роли; допустимо ли иное отнесение.
4. Опора на участников синтеза: какие положения участников (или зерна концепции) поддерживают категорию, а какие ей сопротивляются.
5. Слабые места: не менее двух уязвимостей обоснования и способы их укрепления.

${FORMAT_FRAGMENT}`,
    description:
      "Enrichment (5.3): обоснование категории — необходимость, определение, тип и роли, опора на участников (тип justification)",
  },
  {
    key: "enrichment.edge.justification",
    body: `Задача: философское обоснование одной связи между категориями синтезированной концепции.

${CONTEXT_BLOCK}

СВЯЗЬ: «{{source_name}}» → «{{target_name}}»
Тип связи: {{edge_type}}; направленность: {{edge_direction}}
Описание (из графа): {{edge_description}}
Характеристики связи: {{edge_metrics}}
Категория-источник «{{source_name}}»: {{source_definition}}
Категория-цель «{{target_name}}»: {{target_definition}}

Требуется (каждый пункт — отдельный <h5>, не менее 120 слов):
1. Философское обоснование: почему между категориями существует именно такая связь; какие определения и роли категорий её порождают.
2. Обоснование типа и направленности: почему связь отнесена к типу «{{edge_type}}» и направлена так, а не иначе; есть ли основания для обратного или двунаправленного чтения.
3. Аналоги в других системах: не менее трёх философских систем, где сходная связь между сходными понятиями уже проведена, — с указанием сходства и различия.
4. Следствия: какие тезисы или другие связи концепции опираются на эту связь.

${FORMAT_FRAGMENT}`,
    description:
      "Enrichment (5.3): обоснование связи — основания, тип и направленность, аналоги в других системах (тип justification)",
  },
  {
    key: "enrichment.edge.counterarguments",
    body: `Задача: контраргументы к одной связи между категориями синтезированной концепции — систематическая проверка на прочность.

${CONTEXT_BLOCK}

СВЯЗЬ: «{{source_name}}» → «{{target_name}}»
Тип связи: {{edge_type}}; направленность: {{edge_direction}}
Описание (из графа): {{edge_description}}
Характеристики связи: {{edge_metrics}}
Категория-источник «{{source_name}}»: {{source_definition}}
Категория-цель «{{target_name}}»: {{target_definition}}

Требуется (каждый пункт — отдельный <h5>, не менее 120 слов):
1. Контраргументы против существования связи: не менее трёх, каждый — с позиции конкретной традиции или участника синтеза; указать, какое допущение концепции атакует каждый.
2. Контраргументы против типа и направленности: почему связь можно счесть иной по типу или обратной по направлению.
3. Ответы концепции: как концепция может парировать каждый контраргумент, не меняя определений категорий; где парирование невозможно.
4. Итог: остаётся ли связь обоснованной; какую характеристику (сила, определённость, логическая необходимость) следует скорректировать и в какую сторону.

${FORMAT_FRAGMENT}`,
    description:
      "Enrichment (5.3): контраргументы к связи — против существования, типа, направленности; ответы концепции (тип counterarguments)",
  },
  {
    key: "enrichment.characteristic_justification",
    body: `Задача: обоснование числовой характеристики элемента синтезированной философской концепции.

${CONTEXT_BLOCK}

ЭЛЕМЕНТ: {{element_kind}} «{{element_label}}»
Содержание: {{element_summary}}

Я определил для этого элемента характеристику «{{characteristic_label}}» ({{characteristic_key}}) = {{value}} (допустимый диапазон {{range}}; текущее значение в документе — {{current_value}}).

Предложи философские основания для такой оценки, ограничения и альтернативные подходы к оценке. Каждый из трёх блоков — не менее 100 слов, с опорой на определения, связи и роли из графа концепции и на позиции участников синтеза.

ФОРМАТ ОТВЕТА: ТОЛЬКО HTML-фрагмент из трёх именованных секций строго в этом порядке:
<div data-section="${JUSTIFICATION_SECTIONS.justification}"><h4>${JUSTIFICATION_SECTIONS.justification}</h4>…почему оценка {{value}} философски и методологически обоснована…</div>
<div data-section="${JUSTIFICATION_SECTIONS.limitations}"><h4>${JUSTIFICATION_SECTIONS.limitations}</h4>…что оценка не учитывает; при каких прочтениях концепции она неверна…</div>
<div data-section="${JUSTIFICATION_SECTIONS.alternatives}"><h4>${JUSTIFICATION_SECTIONS.alternatives}</h4>…иные способы оценить эту характеристику и какие значения они дали бы…</div>
Внутри секций — <p>, <ul>/<ol> с <li>, <strong>, <em>. Значения атрибута data-section — ДОСЛОВНО как указано. Никакого текста вне трёх секций.`,
    description:
      "Enrichment (5.3): обоснование числовой характеристики категории/связи — основания, ограничения, альтернативные подходы (три data-section → колонки characteristic_justifications)",
  },
];
