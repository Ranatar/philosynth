/**
 * Промптовые шаблоны Representation Transformer (беседа 5.5; 01-arch
 * §4.11, 03-spec §2.15). НОВЫЕ тексты: в исходнике подсистемы нет,
 * генератора для них нет — файл правится вручную и сеется
 * scripts/seed-prompts.ts вместе с 0.3/1.2/5.3 (образец —
 * enrichment-templates.ts).
 *
 * Ключи: transform.graph_to_theses, transform.theses_to_graph.
 *
 * Устройство промпта (решение 5.5): шаблон задаёт ТОЛЬКО режим
 * трансформации — источник (граф или тезисы из БД) как единственный вход
 * и запрет на что-либо вне него. Форма результата в шаблоне НЕ
 * описывается: она подставляется в {{section_task}} готовым заданием
 * раздела из buildSectionDefs (section-defs-builder 1.2) — той же
 * структурой подразделов и столбцов таблиц, что у обычной генерации
 * (extGraphMetrics, столбец уровня, роли метода, кардинальные словари).
 * Поэтому ответ парсится штатными parseGraphFromHTML / parseThesesFromHTML
 * и без адаптаций замещает html_content раздела.
 *
 * Системный промпт — buildSYS(p) с outputMode 'full' (ответ — целый
 * <div class="doc-section"> раздела). Язык вывода — lang синтеза через
 * system.lang_instruction.
 *
 * Плейсхолдеры (все обязательны — renderTemplate strict):
 *   общие:       {{synthesis_context}} {{section_task}}
 *   graph→theses: {{graph_block}}
 *   theses→graph: {{theses_block}}
 */
import type { SeedPromptTemplate } from "./prompt-templates.js";

const CONTEXT_BLOCK = `КОНТЕКСТ СИНТЕЗА (только для ориентации — не источник содержания):
"""
{{synthesis_context}}
"""`;

export const SEED_TRANSFORM_TEMPLATES: SeedPromptTemplate[] = [
  {
    key: "transform.graph_to_theses",
    body: `ЗАДАЧА: ТРАНСФОРМАЦИЯ ПРЕДСТАВЛЕНИЯ — граф категорий → корпус тезисов.

Это НЕ перегенерация раздела по контексту документа. Это прямая конверсия структурного представления концепции (категории, связи, кластеры, роли) в пропозициональное (утверждения с обоснованиями). ЕДИНСТВЕННЫЙ источник содержания — граф ниже. Зерно, прочие разделы документа и прежние тезисы использовать ЗАПРЕЩЕНО: каждый тезис обязан логически следовать из структурных отношений между категориями графа и быть прослежен к конкретным категориям и связям (с указанием их названий и типов связей).

${CONTEXT_BLOCK}

ГРАФ КАТЕГОРИЙ (источник трансформации):
"""
{{graph_block}}
"""

ПРАВИЛА КОНВЕРСИИ:
- Каждая связь высокой силы (≥ 0.7) и каждый мост между кластерами должны порождать хотя бы один тезис; центральные категории — не менее двух тезисов.
- Тип тезиса (онтологический / эпистемологический / этический) выводится из типов участвующих категорий: онтологические, метафизические категории → онтологические тезисы; эпистемологические, логические, феноменологические → эпистемологические; этические, аксиологические, практические, социальные, политические → этические.
- Формулировка — одно предложение, утверждение, не описание категории; обоснование — ссылка на связи и роли графа, а не на внешние источники.
- В столбце «Связанные категории» сводной таблицы — ТОЧНЫЕ названия категорий из графа.
- Категории, не вошедшие ни в один тезис, недопустимы: покрой весь граф.

ЗАДАНИЕ (форма результата — строго по нему, без добавления других разделов):

{{section_task}}`,
    description:
      "Representation Transformer (5.5): граф → тезисы; вход — только граф из БД (01-arch §4.11), форма результата — задание раздела theses из buildSectionDefs",
  },
  {
    key: "transform.theses_to_graph",
    body: `ЗАДАЧА: ТРАНСФОРМАЦИЯ ПРЕДСТАВЛЕНИЯ — корпус тезисов → граф категорий.

Это НЕ перегенерация раздела по контексту документа. Это прямая конверсия пропозиционального представления концепции (утверждения с обоснованиями) в структурное (категории, связи, кластеры, роли). ЕДИНСТВЕННЫЙ источник содержания — тезисы ниже. Зерно, прочие разделы документа и прежний граф использовать ЗАПРЕЩЕНО: каждая категория и каждая связь обязаны выводиться из конкретных тезисов и быть к ним прослежены (номер тезиса — в столбце происхождения категории и в описании связи).

${CONTEXT_BLOCK}

КОРПУС ТЕЗИСОВ (источник трансформации):
"""
{{theses_block}}
"""

ПРАВИЛА КОНВЕРСИИ:
- Категории — понятия, которые тезисы утверждают, связывают или предполагают; названия — по «Связанным категориям» тезисов, где они даны, иначе — 1–4 слова по формулировке.
- Каждый тезис порождает хотя бы одну связь между категориями; тип связи выводится из логической формы тезиса (обусловливание → каузальная; включение → часть-целое / иерархическая; напряжение → противоречие / диалектическая; взаимное определение → дополнительность и т.д.).
- Кластеры — группы категорий, задействованных общей группой тезисов (онтологической, эпистемологической, этической) либо общей темой; роли категорий — по частоте и позиции в тезисах (центральная — в наибольшем числе тезисов, мост — связывает тезисы разных групп).
- Тезисы, не отражённые ни одной категорией или связью, недопустимы: покрой весь корпус.

ЗАДАНИЕ (форма результата — строго по нему, без добавления других разделов):

{{section_task}}`,
    description:
      "Representation Transformer (5.5): тезисы → граф; вход — только тезисы из БД (01-arch §4.11), форма результата — задание раздела graph из buildSectionDefs",
  },
];

/** Ключ шаблона по направлению. */
export function transformPromptKey(
  direction: "graph_to_theses" | "theses_to_graph",
): string {
  return `transform.${direction}`;
}
