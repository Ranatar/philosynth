#!/usr/bin/env python3
"""Четвёртый предпатч доков: долги журнала, потерявшие адресата.

Идемпотентный: каждая правка сперва проверяет наличие нового текста
(applied ранее -> skip), затем наличие якоря (нет -> FAIL). str.replace,
НЕ re.sub. Запуск из корня репо:
    python3 scripts/patch-docs-debts-pre.py

ПОРЯДОК: применяется ПОСЛЕ patch-docs-conv16-pre.py,
patch-docs-conv23-pre.py и patch-docs-conv46-pre.py — проверка всех
трёх встроена.

Источник: РУЧНАЯ сверка NEXT-CONTEXT ↔ 07 по разделам «Открытые TODO
после X» и «Помодульно» всех глав журнала. Машинная проверка (третий
предпатч) этого не видела: она искала файлы и эндпоинты, а долги в
журнале записаны именами функций и формулировками задач.

Найденное — не дыры в составе бесед, а разрыв цепочки переадресации:
долг заводится в одной беседе, адресуется другой, та его не делает и
переадресует дальше; на втором-третьем шаге адрес размывается или
теряется. В 07 таких долгов не оказалось вовсе:

  1. setPlanResumeExecutor — TODO(2.2) из главы 1.4b. pause-resume-service
     ждёт регистрации исполнителя планов; без неё resume_plan с
     retry/skip_step отдаёт RESUME_INVALID. Беседа 2.2 не знала.
  2. registerParentContextProvider — TODO(3.1) из главы 1.4. В коде
     стоит заглушка, которую должна заменить 3.1. Беседа 3.1 не знала.
  3. applyReplacement / updateCompatAdvisor / toggleCompatPanel — заведены
     в 1.1, адресованы 1.5, в 1.5 не сделаны, переадресованы в «TODO(2.x)»
     БЕЗ НОМЕРА беседы. В 07 не упоминались ни разу. Отданы беседе 3.2 —
     единственной непроведённой, которая правит SynthesisForm.
  4. «Ввод ключа в auth-модалке» — TODO(6.1) из главы 1.4b: задача
     клиентская (форма ввода ключа в PauseModal), а адресована серверной
     беседе 6.1. Отдана клиентской 6.2.
  5. «Снять гейт мета-синтеза в SynthesisForm» — TODO(3.1) из главы 1.5b.
     Серверная половина (приём участников-концепций) — беседа 3.1,
     клиентская (снятие блокировки сабмита) уже вписана в 3.2 вторым
     предпатчем; связь между половинами теперь названа явно.
  6. parentFieldsUsed — упоминался только в беседе 1.3 (закрыта), хотя
     наполнения полей журнал 1.4 требует от 3.1.
  7. estimate-diff в FullBudgetPreview — упоминался только в 1.5
     (закрыта), хотя журнал 1.5b адресует его 3.1. Разведён: данные —
     3.1, отрисовка — 3.2.

  ИСПРАВЛЕНИЕ ВТОРОГО ПРЕДПАТЧА
  8. restoreCapsulesFromHTML: журнал 1.5b адресует его беседе 3.2, а
     второй предпатч написал в 07 «остаётся отложенным до 4.3» — расхождение,
     внесённое без сверки с журналом. Возвращён беседе 3.2.

  ПРОФИЛАКТИКА
  9. Новый раздел 07 §12 «Реестр открытых долгов» — сводная таблица
     «долг → адресат → источник». Пока адреса жили только в семи главах
     журнала, потеря адресата не была заметна ни одной проверкой.
"""
from pathlib import Path

applied, skipped, failed = [], [], []

PROTOCOL = "docs/07-conversation-protocol.md"
GUARDS = [
    ("### Беседа 1.6b: Просмотр документа + каталог (клиент)", "patch-docs-conv16-pre.py"),
    ("беседу 2.4 стоит вести ПЕРЕД 2.3", "patch-docs-conv23-pre.py"),
    ("6. server/routes/prompts.ts (03 §2.9 Prompts + §2.11 Configs)", "patch-docs-conv46-pre.py"),
]


def patch(path: str, old: str, new: str, tag: str, marker: str | None = None) -> None:
    """Точечная замена; probe (по умолчанию — новый текст) даёт идемпотентность."""
    p = Path(path)
    if not p.exists():
        failed.append(f"{tag}: файл не найден — {path}"); return
    s = p.read_text(encoding="utf-8")
    probe = marker if marker is not None else new
    if probe in s:
        skipped.append(tag); return
    if old not in s:
        failed.append(f"{tag}: якорь не найден в {path}"); return
    p.write_text(s.replace(old, new, 1), encoding="utf-8")
    applied.append(tag)


def check_order() -> bool:
    p = Path(PROTOCOL)
    if not p.exists():
        failed.append(f"порядок: не найден {PROTOCOL}"); return False
    s = p.read_text(encoding="utf-8")
    miss = [name for probe, name in GUARDS if probe not in s]
    if miss:
        failed.append("порядок: сперва нужно применить " + ", ".join(miss))
        return False
    return True


if not check_order():
    print("APPLIED: 0"); print("SKIPPED: 0"); print("FAILED:", len(failed))
    for _t in failed: print("  !", _t)
    raise SystemExit(1)


# ══════════════════════════════════════════════════════════════════
# A. Беседа 2.2 — setPlanResumeExecutor
# ══════════════════════════════════════════════════════════════════
patch(
    PROTOCOL,
    """   - POST /syntheses/:id/regenerate-subsection
```""",
    """   - POST /syntheses/:id/regenerate-subsection

6. setPlanResumeExecutor — регистрация исполнителя планов в
   pause-resume-service (долг TODO(2.2) главы 1.4b; до аудита
   2026-07-31 в протоколе не значился):
   сейчас resume_plan с retry / skip_step отдаёт RESUME_INVALID с
   пояснением, потому что исполнителя некому вызвать. После
   plan-executor.ts (пункт 2) зарегистрировать его и убрать заглушку;
   проверить, что пауза внутри исполнения плана возобновляется, а не
   только пауза внутри генерации раздела.
```""",
    "debts/A 07 §2.2 setPlanResumeExecutor",
    marker="setPlanResumeExecutor — регистрация исполнителя планов",
)

# ══════════════════════════════════════════════════════════════════
# B. Беседа 3.1 — провайдер родителей, parentFieldsUsed, участники
# ══════════════════════════════════════════════════════════════════
patch(
    PROTOCOL,
    """     передавать в generateSynthesis.
```""",
    """     передавать в generateSynthesis.

6. Долги журнала, адресованные этой беседе (аудит 2026-07-31 — в
   протоколе они не значились, хотя в NEXT-CONTEXT адресованы 3.1):
   - registerParentContextProvider: заменить стаб, зарегистрированный
     беседой 1.4, настоящим провайдером родительского контекста
     (TODO(3.1) главы 1.4). Пока стоит заглушка, parent-context
     отдаёт пустоту, и мета-синтез собирает промпт без родителей.
   - Наполнение parentFieldsUsed, conceptBlockSizes и
     parentSpecBySection реальными значениями (TODO(3.1) главы 1.4;
     parentFieldsUsed до сих пор упоминался только в беседе 1.3).
   - Серверная поддержка участников-концепций: приём type="synthesis"
     в participants. Это снимает гейт мета-синтеза, поставленный
     беседой 1.5b в SynthesisForm — клиентская половина снятия
     вписана в беседу 3.2, серверная делается здесь. Без этой пары
     форма продолжит блокировать сабмит с ☑-концепциями.
   - Данные для estimate-diff в FullBudgetPreview (TODO(3.1) главы
     1.5b): оценка с учётом участников-концепций; отрисовка разницы —
     беседа 3.2.
```""",
    "debts/B 07 §3.1 провайдер родителей, parentFieldsUsed, участники",
    marker="registerParentContextProvider: заменить стаб",
)

# ══════════════════════════════════════════════════════════════════
# C. Беседа 6.2 — форма ввода API-ключа (клиентская половина TODO(6.1))
# ══════════════════════════════════════════════════════════════════
patch(
    PROTOCOL,
    """5. Защита роута: AdminPromptsPage доступна только для role === 'admin'
```""",
    """5. Защита роута: AdminPromptsPage доступна только для role === 'admin'

6. Форма ввода API-ключа в auth-модалке (долг TODO(6.1) главы 1.4b —
   задача клиентская, но была адресована серверной беседе 6.1; аудит
   2026-07-31):
   в client/src/components/synthesis/PauseModal.tsx auth-рендерер
   сейчас предлагает только «Повторить» (после замены ключа на
   сервере) и «Остановить» — порт _resumeWithNewApiKey [25028]
   опущен. После BYO-Key из 6.1 ключ становится пользовательским,
   и форму можно показать прямо в модалке паузы.
```""",
    "debts/C 07 §6.2 форма ввода API-ключа в auth-модалке",
    marker="Форма ввода API-ключа в auth-модалке",
)

# ══════════════════════════════════════════════════════════════════
# D. Беседа 3.2 — кнопки замен CompatAdvisor + estimate-diff
# ══════════════════════════════════════════════════════════════════
patch(
    PROTOCOL,
    """   - getAncestors(id, depth), getDescendants(id, depth), searchByPhilosophers(names)
```""",
    """   - getAncestors(id, depth), getDescendants(id, depth), searchByPhilosophers(names)

7. Долги SynthesisForm, оставшиеся без адресата (аудит 2026-07-31):
   - updateCompatAdvisor / toggleCompatPanel / applyReplacement —
     кнопки применения замен в CompatAdvisor. Заведены беседой 1.1,
     адресованы 1.5, в 1.5 не сделаны и переадресованы в «TODO(2.x)»
     БЕЗ номера беседы; в протоколе не упоминались ни разу. Отданы
     сюда как единственной непроведённой беседе, которая правит
     SynthesisForm.tsx. Сервер (POST /syntheses/advice) готов с 1.5 —
     нужна только клиентская часть: панель, кнопка применения и
     пересчёт выбранных разделов.
   - estimate-diff в FullBudgetPreview (TODO(3.1) главы 1.5b):
     отрисовка разницы оценки с участниками-концепциями и без.
     Данные приходят из беседы 3.1.
```""",
    "debts/D 07 §3.2 кнопки замен CompatAdvisor и estimate-diff",
    marker="кнопки применения замен в CompatAdvisor",
)

# ══════════════════════════════════════════════════════════════════
# E. Исправление второго предпатча: restoreCapsulesFromHTML
# ══════════════════════════════════════════════════════════════════
patch(
    PROTOCOL,
    "     `restoreCapsulesFromHTML` остаётся отложенным до 4.3",
    "   - Порт `restoreCapsulesFromHTML`: журнал 1.5b адресует его ЭТОЙ\n"
    "     беседе (TODO(3.2)). Второй предпатч ошибочно отложил его до 4.3 —\n"
    "     исправлено аудитом 2026-07-31. За беседой 4.3 остаётся серверный\n"
    "     импорт файлов, а не восстановление капсул в клиентском пуле.",
    "debts/E 07 §3.2 restoreCapsulesFromHTML возвращён беседе 3.2",
    marker="Второй предпатч ошибочно отложил его до 4.3",
)

# ══════════════════════════════════════════════════════════════════
# F. Новый раздел 07 §12 — реестр открытых долгов
# ══════════════════════════════════════════════════════════════════
REGISTRY = """

---

## 12. Реестр открытых долгов

Заведён аудитом 2026-07-31. До него адреса долгов жили только в семи
главах `NEXT-CONTEXT.md` (разделы «Открытые TODO после X»), и потерю
адресата не ловила ни одна проверка: долг заводится в одной беседе,
адресуется другой, та его не делает и переадресует дальше — на втором
шаге адрес размывается («TODO(2.x)») или исчезает.

**Правило:** беседа, оставляющая долг, обязана внести строку СЮДА, а не
только в свою главу журнала. Беседа, закрывающая долг, вычёркивает
строку. Адрес без номера беседы («2.x», «позже», «когда появится») не
допускается — если адресата нет, долг остаётся за текущей беседой.

| Долг | Адресат | Заведён | Состояние |
|---|---|---|---|
| `canonicalSubsectionKey` — каноникализация ключей подразделов | 2.1 | 1.3 | в тексте 2.1 |
| `estimateCascadeWaveCost` / `formatWaveCost` | 2.1 | 1.1 | в тексте 2.1 |
| `setPlanResumeExecutor` — регистрация исполнителя планов | 2.2 | 1.4b | внесён 2026-07-31 |
| Полный `regenerateSubsection` + plan-executor | 2.2 | 1.4b | в тексте 2.2 |
| `confirm` деградации зависимостей при skip | 2.2 | 1.4b (адресовался 1.5) | внесён 2026-07-30 |
| Бейдж качества контекста (`contextQualityScore`) | 2.3 ← 2.4 | 1.3 | инверсия снята 2026-07-30 |
| `context-quality.ts` / `getSectionContextQuality` | 2.4 | 1.3 | в тексте 2.4 |
| `registerParentContextProvider` — реальный провайдер | 3.1 | 1.4 | внесён 2026-07-31 |
| `parentFieldsUsed` / `conceptBlockSizes` / `parentSpecBySection` | 3.1 | 1.4 | внесён 2026-07-31 |
| Серверные участники-концепции (снятие гейта мета-синтеза) | 3.1 + 3.2 | 1.5b | внесён 2026-07-31 |
| Данные для `estimate-diff` | 3.1 | 1.5b | внесён 2026-07-31 |
| `reconstructGenealogy` | 3.2 | 1.5b | в тексте 3.2 |
| `restoreCapsulesFromHTML` | 3.2 | 1.5b | адрес восстановлен 2026-07-31 |
| `applyReplacement` / `updateCompatAdvisor` / `toggleCompatPanel` | 3.2 | 1.1 (адресовался 1.5, затем «2.x») | внесён 2026-07-31 |
| Отрисовка `estimate-diff` в `FullBudgetPreview` | 3.2 | 1.5b | внесён 2026-07-31 |
| Серверный импорт концепт-файлов | 4.3 | 1.5b | в тексте 4.3 |
| `reconstructSkeleton` как fallback в `formatPromptsForExport` | 4.2 → 2.4 | 2.4 | инверсия снята 2026-07-30 |
| BYO-Key (ключ пользователя вместо env) | 6.1 | 1.4 | в тексте 6.1 |
| Форма ввода ключа в auth-модалке `PauseModal` | 6.2 | 1.4b (адресовался 6.1) | внесён 2026-07-31 |
| Маркеры `TODO(1.6)` — развести между 1.6 и 1.6b | 1.6 + 1.6b | разделение беседы | внесён 2026-07-30 |

Долги, снятые как «не долг»: `POST /auth/password-reset/*` — вне MVP,
помечено в 03 §2.1; `POST /syntheses/estimate` и `/advice` — реализованы
беседой 1.5, зафиксировано записью ревизии.
"""


def append_registry() -> None:
    tag = "debts/F 07 §12 реестр открытых долгов"
    p = Path(PROTOCOL)
    s = p.read_text(encoding="utf-8")
    if "## 12. Реестр открытых долгов" in s:
        skipped.append(tag); return
    p.write_text(s.rstrip("\n") + REGISTRY + "\n", encoding="utf-8")
    applied.append(tag)


append_registry()

# ══════════════════════════════════════════════════════════════════
# G. Чек-лист беседы (§10) — обязанность вносить долг в реестр
# ══════════════════════════════════════════════════════════════════
patch(
    PROTOCOL,
    "## 10. Контрольный чек-лист для каждой беседы\n",
    "## 10. Контрольный чек-лист для каждой беседы\n"
    "\n"
    "> **Добавлено 2026-07-31:** завершая беседу, внеси каждый оставленный\n"
    "> долг в §12 «Реестр открытых долгов» с ЯВНЫМ номером беседы-адресата,\n"
    "> а закрытый — вычеркни оттуда. Записи только в главе NEXT-CONTEXT\n"
    "> недостаточно: именно так семь долгов потеряли адресата.\n",
    "debts/G 07 §10 обязанность вести реестр долгов",
    marker="внеси каждый оставленный\n> долг в §12",
)

# ══════════════════════════════════════════════════════════════════
# H. NEXT-CONTEXT: глава
# ══════════════════════════════════════════════════════════════════
CHAPTER = """

---

# Сверка NEXT-CONTEXT ↔ 07: потерянные адресаты [ЗАКРЫТА]

Четвёртый и последний предпатч аудита: `scripts/patch-docs-debts-pre.py`
(после 16pre → 23pre → 46pre, проверка порядка встроена).

## Почему понадобился

Первые три предпатча опирались в основном на механическую сверку — пути
файлов и эндпоинты. Она не видит долгов: те записаны в журнале именами
функций и формулировками задач («снять гейт мета-синтеза», «ввод ключа
в auth-модалке»). Ручное чтение разделов «Открытые TODO после X» всех
семи глав дало семь долгов, которых в 07 не было НИ РАЗУ.

## Механизм потери

Долг заводится беседой A, адресуется беседе B, беседа B его не делает и
переадресует беседе C. На втором шаге адрес размывается, на третьем
исчезает. Крайний случай:

    applyReplacement / updateCompatAdvisor / toggleCompatPanel
    1.1 → «клиент, 1.5» → 1.5 не сделала → «TODO(2.x)» → нигде

«2.x» — не адрес. В протоколе этих функций не было ни разу, при том что
серверная половина (`POST /syntheses/advice`) готова с беседы 1.5.

## Что внесено

| Долг | Куда отдан |
|---|---|
| `setPlanResumeExecutor` | 2.2 |
| `registerParentContextProvider`, `parentFieldsUsed`, `conceptBlockSizes`, `parentSpecBySection` | 3.1 |
| Серверные участники-концепции (снятие гейта мета-синтеза) | 3.1 (сервер) + 3.2 (клиент) |
| Данные и отрисовка `estimate-diff` | 3.1 + 3.2 |
| `applyReplacement` / `updateCompatAdvisor` / `toggleCompatPanel` | 3.2 |
| Форма ввода ключа в auth-модалке | 6.2 |

Выбор адресатов: 3.2 — единственная непроведённая беседа, правящая
`SynthesisForm.tsx`; 6.2 — единственная клиентская в Фазе 6.

## Исправление второго предпатча

`restoreCapsulesFromHTML`: журнал 1.5b адресует его беседе 3.2, а второй
предпатч написал в 07 «остаётся отложенным до 4.3» — расхождение,
внесённое без сверки с журналом. Возвращён беседе 3.2; за 4.3 остаётся
серверный импорт файлов.

## Профилактика: 07 §12 «Реестр открытых долгов»

Сводная таблица «долг → адресат → кем заведён → состояние», 20 строк.
В §10 (чек-лист беседы) добавлена обязанность вносить долг в реестр с
ЯВНЫМ номером адресата и вычёркивать закрытый. Адрес без номера
(«2.x», «позже») запрещён: если адресата нет, долг остаётся за текущей
беседой.

## Наблюдение

**Беседа 3.1 — узел накопления:** к ней адресуются главы 1.2, 1.3, 1.4 и
1.5b, а её первый запрос в 07 выглядит скромно. Начав её «как написано»,
четыре накопленных требования не всплыли бы вовсе.

## Граница аудита

Проверены состав бесед, связи между ними и адресация долгов.
Содержательная правильность заданий — соответствие первых запросов
исходнику `philosynth.html` — проверялась выборочно, только для 1.6b и
1.7. Это остаётся неохваченным.
"""


def append_chapter() -> None:
    tag = "debts/H NEXT-CONTEXT глава сверки"
    p = Path("NEXT-CONTEXT.md")
    if not p.exists():
        failed.append(f"{tag}: файл не найден"); return
    s = p.read_text(encoding="utf-8")
    if "# Сверка NEXT-CONTEXT ↔ 07: потерянные адресаты [ЗАКРЫТА]" in s:
        skipped.append(tag); return
    p.write_text(s.rstrip("\n") + CHAPTER + "\n", encoding="utf-8")
    applied.append(tag)


append_chapter()

print("APPLIED:", len(applied))
for t in applied: print("  +", t)
print("SKIPPED:", len(skipped))
for t in skipped: print("  =", t)
print("FAILED:", len(failed))
for t in failed: print("  !", t)
raise SystemExit(1 if failed else 0)
