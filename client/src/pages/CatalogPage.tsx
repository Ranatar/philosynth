/**
 * Каталог концепций (свои + публичные). Беседа 0.4: заглушка.
 * Наполнение: беседа 1.6b (SynthesisList, SynthesisCard; CatalogFilters —
 * Фаза 2, поиск живёт в CatalogPage). Транспорт готов (беседа 1.6).
 */
import { PageStub } from "./PageStub";

export function CatalogPage() {
  return <PageStub title="Каталог концепций" plannedIn="1.6b" />;
}
